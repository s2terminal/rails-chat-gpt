<h1>フォーム</h1>

<%= form_tag('/form') do %>
  <%= label_tag(:message, 'メッセージ:') %>
  <%= text_field_tag(:message) %>
  <%= submit_tag('送信') %>
<% end %>

<% if @message.present? %>
  <h2>入力内容</h2>
  <p><%= @message %></p>
<% end %>
